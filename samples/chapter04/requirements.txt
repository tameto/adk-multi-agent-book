google-adk[db,gcp]==2.2.0
google-genai>=2.4,<3
google-cloud-aiplatform==1.153.1   # Vertex AI Memory Bank / RAG Engine用（GAの vertexai.rag を使用）
opentelemetry-exporter-otlp-proto-http==1.41.1
python-dotenv>=1.0.0
pydantic>=2.0.0
asyncpg>=0.29.0      # DatabaseSessionService用（PostgreSQL）
aiosqlite>=0.20.0    # DatabaseSessionService用（SQLite、テストで使用）
greenlet>=3.0.0      # SQLAlchemy async engineが同期処理をブリッジするために必要
pytest>=8.0.0        # tests/ の実行用
pytest-asyncio>=0.24.0  # tests/test_inmemory_session.py（asyncフィクスチャ）の実行用
